export enum TaskStatuses {
  Completed = 2,
  Draft = 3,
  InProgress = 1,
  New = 0,
}

export enum TaskPriorities {
  High = 2,
  Later = 4,
  Low = 0,
  Middle = 1,
  Urgently = 3,
}

export enum ResultCode {
  Captcha = 10,
  Error = 1,
  Succeeded = 0,
}
